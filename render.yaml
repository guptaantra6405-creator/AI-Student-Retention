services:
  - type: web
    name: ai-student-retention
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: FLASK_DEBUG
        value: "false"
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: JWT_EXPIRES_SEC
        value: "86400"
      - key: GOOGLE_CLIENT_ID
        sync: false
